# 当前状态

更新时间：2026-08-18

## 2026-08-18 K 线结构响应与界面层级

- K 线结构和筹码结构的请求已拆分：点击“K线结构”仅复用日 K 计算价格结构，不再等待东方财富或 Baostock 的筹码日线；只有点击“筹码结构”才触发原有双源筹码请求。筹码源可达性仍待单独处理，失败不阻塞短线结构。
- 主图保持无文字：结构结论、支撑压力、箱体和波段中文详情均在主图下方说明区。已确认箱体和未确认观察区统一为带轻微发光的淡蓝色实线/虚线框；顶部导航改为滚动时保持可见的胶囊形导航。
- 自选股可单独标为重点关注：星标行使用淡蓝发光边框，重点状态随浏览器本地自选保存；“取消突出 / 恢复突出”保留全部重点条目，只切换淡蓝发光边框的视觉强调，不影响分组、行情刷新、盯盘规则、风险评分或 SQLite。独立页面交互已验证。
- 大盘页首次绘制以及切回大盘时，行业资金流的左右柱状图会从中轴展开、环形图会从中心淡入放大；该动效仅重播已有快照，不会重新请求、改变排序、颜色、数字或任何资金流口径，且会遵从系统“减少动态效果”设置。窗口尺寸变化只静态重绘。
- 分时累计量比拟按“截至同一分钟的今日累计成交量 / 过去 5 个完整交易日同期累计量均值”计算；当前腾讯历史分钟接口因本机 DNS 不可达，尚未对真实字段完成固定样例核验，故未接入页面。

## 2026-08-17 当前开发分支交接

- K 线结构视图完成仅呈现层的层级收敛：主图不再绘制 HH/HL/LH/LL 波段圆点、英文缩写或
  支撑/压力/区间文字，只保留线和区间的视觉标记；摘要直接显示中文结构结论、确认程度和
  价格，近期波段改为可展开的中文详情。严格震荡箱体使用蓝色实线边框，绿色/黄色观察区
  降低填充并使用虚线边框；未修改波段识别、支撑压力确认、筹码取数或任何计算口径。
- 本次呈现层改动的 `tests.test_analysis` 与 `tests.test_http` 共 66 项通过，`app.py` 编译和
  `git diff --check` 通过。独立端口 `8694` 真实页面验收覆盖 `600519` 的已确认箱体与
  `002273` 的未形成箱体场景：主图不再显示 LH/LL，摘要和展开详情可用，页面无横向溢出。
  验收未调用 AI、未使用或修改 `data/monitor.db`；筹码免费源在验收期间仍可能失败，但不影响
  原始 K 线和结构视图。
- 完整 `unittest discover` 本次运行 190 项，其中 189 项通过；唯一错误为
  `test_first_stage_cannot_see_locked_user_judgment_and_cache_is_reused`，其假模型读取不到预期的
  `pending` 持仓报告记录。该调用链位于未改动的 `monitoring/portfolio.py` 与
  `tests/test_portfolio.py`，需作为独立持仓报告测试问题诊断，不随本次 K 线呈现层调整修改。
- 本次呈现层调整位于独立分支 `codex/kline-presentation-hierarchy`，从
  `codex/market-quadrant-kline-structure-mvp@d6c7cf1` 创建，尚未推送。后者的最新功能提交为
  `e8a73ac`，其本地与 `origin/codex/market-quadrant-kline-structure-mvp` 保持同步；
  `main@4cb4441` 仍保持 `v0.2.0` 基线，尚未合并当前功能；
- K 线结构在严格震荡箱体之外新增保守价格行为回退：显示上行/下行/震荡过渡状态、
  20 日位置、HH/HL/LH/LL 波段点、近期突破状态和观察区。观察区不会冒充已确认支撑或压力；
  相关 4 项定向测试通过；
- 大盘原四象限散点第一版保留在 `6ef5cae` 作为回退点，当前版 `e8a73ac` 已改为
  净流出 TOP 8 / 净流入 TOP 8 对称横向柱状图，以及资金方向与涨跌方向的行业数量环形图；
  柱长使用对数缩放，页面同时显示真实亿元数；
- 行业资金为最近完整快照时，只显示黄色状态提示，不再对行业涨跌区整体降透明或灰化。
  当前页面已在 `540×672` 和 `1280×720` 检查：SVG 非空、行业红绿原色正常、无横向溢出、
  文字未超出图形区域、控制台无警告或错误；首页前端契约定向测试 1 项通过；
- `feat/key-level-view-toggle@2f48467`、`feature/industry-fund-flow@97ebf26` 和父功能分支
  `codex/market-quadrant-kline-structure-mvp` 均已推送远端，本地/远端差异为 `0/0`。父功能分支
  已经包含前两项功能提交，后续接手不要从旧功能分支重复开发，也不要因其分支名仍含
  `market-quadrant` 而误判当前页面仍是四象限。

## 2026-08-17 行业资金流免费主备源

- 实测东方财富行业资金流连续 `0/8` 成功；同花顺高频连续请求 `6/8` 成功，按项目实际
  低频间隔请求 `4/4` 成功。因此行业资金净额改为同花顺主源，东方财富完整行业主力净流入
  降级为回退源；两者都失败时保留最近完整快照，再无快照才回退板块 ETF 涨跌；
- 保留既有字段语义：`main_net` 仍只表示东方财富主力净流入；新增
  `flow_net/flow_kind/flow_source/flow_label` 表示当前实际资金口径。同花顺净额不会伪装成
  东方财富主力净流入，也不会以 `main_net_inflow_yi` 名义进入 AI 事实；
- 同花顺签名使用固定版本的 MIT 许可脚本和轻量 `mini-racer==0.14.1`，没有引入完整 AkShare。
  本次只替换行业资金流；PE/PB、F10、ETF 披露、个股资金流和筹码数据源均未改变；
- 行业/分析/HTTP 定向测试 `85` 项全部通过，`app.py` 编译、内嵌 JavaScript 语法和
  `git diff --check` 通过。完整回归 `188` 项、`187` 项通过；唯一错误是既有持仓报告测试
  固定在 `2026-08-04` 创建 7 天记录，却按当前日期查询导致过期，与本轮文件和调用链无关；
- 独立端口真实服务返回 `flow_source=ths`、`flow_complete=true`，共 `90` 个行业，其中
  `52` 个净流入、`38` 个净流出。浏览器显示“同花顺行业资金净额”，完整榜可展开且包含
  负值净流出；页面无横向溢出，控制台无警告或错误，SVG 资金流图非空且粒子位置持续变化。
  测试使用临时快照和端口 `8691`，未操作 `8688`，未读取或修改 `data/monitor.db`。

## 2026-08-14 行业资金流与动效

- 大盘板块数据使用东方财富官方行业页参数，分别获取净流入/净流出前 50 并按代码合并；
  市场响应保留 `code/name/chg` 兼容字段，新增 `main_net/main_pct/stale/source/flow_complete`。
  只有同时包含净流入和净流出时才认定资金流完整；旧单边快照只保留涨跌幅，不再展示资金流排行，
  也不进入 AI 资金流证据；
- 大盘粒子固定由浏览器原生 RAF 驱动，避免不同浏览器的 GSAP 加载差异；单标的资金流动效仍优先
  使用 GSAP ticker、不可达时回退 RAF。行业涨跌区默认显示市场宽度、涨跌幅前 10，全部行业榜
  折叠保留并按涨幅从高到低排序；只有 `flow_complete=true` 时才同时显示主力净流入（亿元）；
- 大盘 AI 与单标的 AI 的行业资金流证据均明确主力净流入口径与数据延迟状态；
- 首页改为快照优先：页面请求不再同步等待东财，而是先返回最近快照，再由单一后台线程
  并行更新净流入/净流出两端。更新失败时保留上一份完整榜单，手动刷新也不会清空页面；
  行业缓存周期为 300 秒，页面显式标记“后台更新中”、“最近完整快照”或“资金流暂不可用”；
- `启动.bat` 固定优先使用 Microsoft Edge 打开页面，Edge 不可用时回退系统默认浏览器；
- 本轮行业/分析/HTTP 定向回归 80 项通过；`app.py` 编译与内嵌 JavaScript 语法通过。真实东财
  单次全量请求仍报 `Remote end closed connection without response`，免费实时源可达性不能保证；
  当前 2026-08-14 16:40 本地快照只含单边数据，因此仍只显示行业涨跌，待后台首次取得双向完整结果后
  才会建立可持续回退的资金流榜单；
- 交付包 12 项行业测试与行业/分析/HTTP 合计 72 项通过；完整回归 175 项、174 项通过，
  唯一失败仍是固定 `2026-08-04` 的持仓报告过期测试，与本分支无关；真实 `1280×720` 独立端口验收
  覆盖实时行业数据、快照延迟、排序、Canvas 粒子和控制台无错误。浏览器环境未加载 GSAP
  CDN 时，RAF 降级路径已实际渲染；
- `data/industry_flow_snapshot.json` 为运行时缓存，已忽略且不提交；本轮为页面验收重启了 `8688`，
  并将服务改为端口独占，避免重复启动后多个 Python 进程混合响应。真实浏览器确认手动刷新时
  100 行行业榜保持显示，刷新失败后状态正常回落；`473×672` 视口无横向溢出。未读取或修改
  `data/monitor.db`。

## 可运行功能

- 本地 Web 分析台、单标的查询、大盘、自选分组追踪、Excel 导出；
- 可选 DeepSeek AI 助手、大盘解释、多股对比和可选 GPT 首席；多维分析页面入口已下线，
  旧后端接口暂作兼容保留；
- 单标的“分析报告”新增按需 DeepSeek 独立分析：模型读取编号事实目录后自行选择重点、
  顺序和表达，后台只校验引用编号；原规则报告折叠保留为零 Token 数据底稿；
- AI 大盘报告改为开放式复盘：模型自行识别指数/板块结构和矛盾，不使用固定栏目模板，
  同样要求引用编号事实；
- 本地 SQLite 自选盯盘、确定性规则、事件记录、企微/Server酱通知适配；
- 三线快速设置：关注价、风险价、可选目标价和自动 `±3%` 异动；
- Web 盯盘标签：三线设置、启停、运行状态、数据计数和最近提醒；
- 按需 AI 规则草案，结果缓存且不自动启用；
- 自选页内的持仓截图导入：千问视觉识别、人工校对和本地持仓写入；
- 持仓页可先执行 0 Token 组合扫描，再按需选择 DeepSeek 或 GPT 生成两阶段报告；
  GPT 默认 `gpt-5.6-sol`，最近 7 次成功分析可切换复盘；
- GPT 持仓报告已改用 Responses API 的严格 JSON Schema 输出，分别锁定独立分析和
  用户判断对照的既有字段；保留原有本地校验。HTTP 400 会显示 OpenAI 的具体错误信息。
  未用真实 OpenAI Key 验证，仍待一次用户主动发起的实际调用确认。
- 右下角 AI 助手支持可选 PNG/JPG 图片附件，也可直接在聊天输入框粘贴截图：带图问题先
  复用持仓截图识别已保存的千问 Key/Base URL 生成自由格式的图片观察记录，再交由 DeepSeek
  解读；原图和千问识别结果均不写入数据库，原图也不会在后续对话中重复发送。无图聊天仍只
  调用 DeepSeek。
- ETF 的 `/api/analyze` 新增公开跟踪标的、最近报告期行业配置、前十大持仓和权重；
  单标的页面已显示“ETF 定位与指数追踪”。它不改变估值、技术、风险或盯盘口径；
- 单标的页面新增“今日分时 · 相对强弱”：股票与所属市场参考指数比较，ETF 优先使用公开
  跟踪指数，匹配不到时回退沪深 300；图中显示标的、参考指数、标的均价和成交量；
- 分时独立于主分析加载，暂时失败不会清空或阻塞其他分析。成功缓存 60 秒，失败缓存 15 秒；
- K 线区域提供互斥的“K线结构”和“筹码结构”按需视图；再次点击当前按钮恢复原始 K 线，
  同一标的来回切换复用一次 `/api/key-levels` 结果。ETF 只显示 K 线结构入口；
- K 线结构只显示震荡箱体、可能支撑和可能压力；筹码结构只显示主要估算成本密集区与右侧
  简化筹码条，不再突出 70%估算成本区或估算获利占比。筹码只可交叉验证价格结构，不能
  单独决定支撑压力，也不进入风险评分、盯盘或买卖建议；
- 买一/卖一已从行情响应和页面撤下；布林带已从页面、规则底稿、AI 输入、K 线图、Excel
  和多股技术提示撤下，底层计算与响应字段暂时保留兼容。
- `app.py`、`agent.py` 和三个批处理入口保持可用。

## 2026-08-12 单标的、大盘 AI 与分时第一版调整

- 单标的按需报告新增 `/api/security_report`。输入包含价格、多周期涨跌、PE/PB 分位、
  技术与量能原始指标、资金、财务、行业、大盘快照；ETF 另含跟踪指数、行业配置和
  定期披露持仓；分时可用时另含压缩摘要和最多 8 个代表时点。程序不把既有规则报告、
  风险理由或提醒结论发送给模型；
- 模型可自主选择 2 至 5 个重要问题并组织文章，但涉及数字或事实时必须引用 `E01` 等
  事实编号；不存在的编号会被拒绝。页面可展开查看本次实际引用的数据依据；
- `/api/intraday` 按需读取标的和参考指数截至查询时点的当日分钟行情，按各自昨收归一后
  计算相对强弱。第一版不读取历史分时，不做复杂分时形态、龙头股组合或盘中买卖提示；
- 股票按上市板块选择上证指数、深证成指、创业板指或科创 50。ETF 根据公开跟踪指数名称
  通用匹配，不为固定样例写死代码；匹配失败时明确标为宽基参考；
- AI 不接收整条分钟序列，只接收摘要和最多 8 个代表时点；分时失败时继续使用其他事实，
  不影响单标的主报告；
- 未修改 PE/PB、分位算法、负值处理、前复权、风险评分、资金字段或 ETF 披露口径；
- `tests.test_analysis` 与 `tests.test_http` 共 37 项通过；`app.py` 编译、内嵌 JavaScript
  语法和相关文件 `git diff --check` 通过。固定数据网页在 `1280×720` 与 `390×844`
  浏览器视口下无横向溢出，分时图非空；未调用真实行情或模型，未启动或重启 8688；
- 真实分时接口、ETF 跟踪指数匹配和真实 DeepSeek 解读质量待用户在原网页验收。

## 2026-08-12 按需关键位 MVP

- 新增独立 `GET /api/key-levels?code=...`，不加入主分析请求。成功结果缓存 6 小时，失败
  结果缓存 5 分钟；关键位失败时原 K 线和其他分析继续显示；
- 震荡区间检查最近 40/60/80/100/120 日，要求价格大部分位于区间、上下边界均有多次
  触碰，并排除明显单边趋势；没有明显箱体时不强行画框；
- 普通股票按最近约 120 个交易日的换手衰减和日内价格分布估算筹码。若筹码数据日期与
  页面 K 线不同，或两路前复权收盘价偏差超过 1%，停止叠加筹码；
- 页面最多显示可能支撑、可能压力和主要筹码峰三条线，并以淡色区域显示震荡区间和
  70%估算成本区；右侧显示简化筹码条，摘要显示平均成本、成本区、估算获利占比、日期
  和样本数。再次点击只隐藏图层，不重复请求；
- 8 项离线定向测试通过，`app.py` 编译、内嵌 JavaScript 语法和 `git diff --check`
  通过。固定数据页面已在 `1280×720` 宽屏完成加载/隐藏检查：图表非空、页面无横向
  溢出、浏览器无警告或错误；
- 本次没有调用真实行情或模型，没有启动或重启 8688，也没有读取或修改
  `data/monitor.db`。真实股票数据、除权场景和页面观感由用户在原页面验收。

## 2026-08-13 K 线与筹码双视图

- 原“加载关键位”收口为互斥的“K线结构”和“筹码结构”。默认不请求；当前按钮再次点击
  恢复原始 K 线；同一标的切换视图不重复请求，切换标的后重新按需加载；
- K 线结构仅绘制震荡箱体、可能支撑和可能压力；没有清晰结构时显示“暂无清晰短线结构”，
  不强行画线。筹码结构仅绘制主要估算成本密集区和右侧简化筹码条，并显示日期、样本数及
  “近120日本地估算”边界；前端不再显示 70%估算成本区或估算获利占比；
- 主要筹码峰只按相对现价位置描述为潜在承接观察区或潜在解套压力区。它接近已识别箱体
  边界时只标记“筹码重合，参考增强”，没有价格结构验证时仍只称估算成本密集区；
- 单标的 AI 按需生成报告时复用关键位缓存，仅在普通股票筹码可用且日期/价格一致时增加
  筹码峰、相对现价、结构重合、日期、样本数和估算标签；不传 70%成本区、估算获利占比或
  持有人意图。关键位失败时静默跳过该事实，不影响报告其他输入；
- `tests.test_analysis` 与 `tests.test_http` 共 49 项通过；`app.py` 编译、内嵌 JavaScript
  Node 语法和 `git diff --check` 通过。完整测试运行 152 项，151 项通过；唯一失败是既有
  持仓报告测试以固定 `2026-08-04` 创建 7 天记录、在当前日期查询时已过期，单独运行同样
  失败，与本轮文件和调用链无关；
- 固定数据服务已在独立端口、临时数据库下完成一次 `1280×720` 浏览器验收：股票默认原图、
  双视图互斥、二次点击恢复、来回切换不残留、同标的一次请求、ETF 隐藏筹码入口、接口失败
  保留原图均通过；页面无横向溢出，浏览器无警告或错误。未启动或重启 8688，未调用真实
  行情或模型，未读取或修改 `data/monitor.db`；临时服务和脚本已清理。

## 2026-08-14 双视图恢复与关键位容错

- 本地仓库恢复后当前工作区重新切回 `feat/key-level-view-toggle`；`main@4cb4441` 仍是
  `v0.2.0` 基线，双视图提交仍为该分支上的 `adf024b`，没有合并 `main`；
- K 线结构继续优先使用严格震荡箱体。未形成箱体时，新增最近 80 个交易日的保守波段
  结构回退：局部高低点至少两次确认、两次触碰至少间隔 5 个交易日、近期仍有触碰且距离
  现价不过远时，才分别显示可能支撑或压力；单边走势和无重复确认样例不强行画线；
- 筹码日线优先使用东方财富，单次请求为 2 次；瞬时失败或数据不足时自动改用免费
  Baostock 的前复权日 K 与换手率。仍由本地模型估算，页面会标记实际原始数据源；
  `chip_status=unavailable` 时页面允许显式“重试筹码结构”并只绕过失败缓存，成功缓存和
  同标的双视图切换仍不重复请求；
- `tests.test_analysis` 与 `tests.test_http` 共 59 项通过；完整测试运行 162 项、161 项通过，
  唯一失败仍是固定 `2026-08-04` 的 7 天持仓报告过期测试，与本轮文件和调用链无关；内嵌
  JavaScript 语法检查和 `git diff --check` 通过；
- 独立端口 `1280×720` 验收覆盖股票默认原图、箱体支撑压力、双视图互斥、筹码条和成本
  密集区、二次点击恢复、ETF 隐藏筹码入口、无横向溢出。真实 `600519` 请求同时复现了
  东方财富瞬时断开，页面保留 K 线结构并出现重试入口；固定筹码响应验证了成功视图。未
  重启 8688，未调用模型，也未读取或修改 `data/monitor.db`。

- 本轮独立端口实际查询 `600519` 时，东方财富仍瞬时断开，Baostock 登录也返回网络接收
  错误；页面保留原图并同时显示两路失败原因。免费双源降级的代码与固定样例已验证，真实
  Baostock 上游连通性仍待网络恢复后复验。

## 本轮已完成

- 完成入口、数据流、计算口径、依赖、安全和文件用途审计；
- 增加依赖/忽略规则、隔离测试和完整交接文档；
- 用固定样例确认 PE/PB/价格分位与核心输出结构；
- 清理已被替代的一次性脚本、重复说明、过时图片和 Python 缓存。
- 新增本地盯盘 MVP：分钟循环 0 Token，分钟数据 7 天、事件 365 天、每日快照长期保留。
- 将普通用户入口简化为 `quick-setup` / `overview`；高级规则保留为次级入口。
- AI 草案从最多 6 条收敛为最多 3 条核心规则，旧版草案缓存不再复用。
- 规则草案 Prompt 改为严格输入边界和严格 JSON，增加 `needs_confirmation`；
  当前缓存版本为 `strict-v4-staged`。
- 增加风险提醒模拟预览：复用已保存的风险价规则和真实提醒格式，但不取行情、不写
  事件、不发送通知、不初始化后台服务，Token 消耗为 0。
- 新增“持仓逻辑转化”代码：本地保存买入/持有逻辑、失效条件和复核事项；按需调用
  顶部 DeepSeek Key 生成最多 3 条严格规则草案，可填入三线但不自动启用。
- 基础转化不调用完整分析器，不向模型发送 RSI、MACD、波动率或估值；无可执行数字
  时保留逻辑卡，并且每轮只提出一个关键问题。用户确认后可在本地历史行情中识别前低，
  生成一条待确认风险价草案，正式规则表保持不变。
- 新增手动事件复核代码：只读取已记录事件和本地逻辑卡，同一输入缓存 7 天；默认
  只记录调用和 Token、不限制额度，分钟监听仍为 0 Token。
- 新增可加载的本地交易日历和按失败渠道持久化的通知重试队列。
- 自选股新增浏览器本地分组与组内行情追踪：等权组均/中位涨幅、上涨家数、当日轨迹
  和透明阈值状态；旧自选自动归入“未分组”，分组名称可随时编辑或合并，纯改名
  保留当天轨迹、合并后按新成员重建基线，且不进入盯盘或风险评分。
- K 线 Tooltip 新增按前一交易日收盘计算的日涨跌幅，不改变后端行情字段和分析口径。
- 个股分析保留量比、换手率、振幅、日内区间，以及东方财富 F10 三级行业、前 4 个概念
  和主营摘要；原买一/卖一已撤下。公司定位与概念只辅助理解，不进入估值、风险评分、
  技术信号或买卖条件。
- 自选页新增“持仓”视图：截图先预览，再手工发送给阿里云百炼 `qwen3.7-flash`；
  识别结果必须人工校对后才写入 SQLite。模糊数字保留为空值并标记重点核对。
- 持仓截图支持从微信聊天窗口直接拖入，也保留点击选图；拖入只生成本地预览，仍需
  手工点击发送，不会因拖放动作自动调用模型。
- 持仓截图识别按图片 SHA-256 和模型缓存结构化结果 7 天；缓存不保存原图、API Key
  或 Base URL。相同截图命中缓存时不调用模型，分钟盯盘始终不调用视觉模型。
- 新增必填“我的当前判断”：点击后先写入 SQLite 并锁定；第一轮报告请求只含现有
  持仓分析生成的结构化快照，第二轮才读取用户原文并生成一致点、分歧点和可能遗漏。
- 新增无 Key、0 Token 的持仓扫描：确定性代码计算 5/10/20/60 日组合趋势、相对大盘/
  板块强弱、回撤、波动、期初市值口径的盈亏贡献、集中度、相关性覆盖和持仓强弱；
  聚合指标统一到共同数据截止日，并显式标记缺失、停牌前值填充和覆盖不足。
- 新增最近约 250 个交易日、每 5 日取样的无前视历史诊断重放。它按当前持仓数量静态
  回看，只描述诊断状态与随后 5/10 日表现，不模拟真实买卖、调仓、现金、费用和分红，不是
  交易策略收益回测。
- 独立报告提示词不再写死指标顺序，由模型选择 2 至 5 个最重要问题，并强制区分
  已确认事实、基于数据的推断和缺失信息；不扩展新闻或其他数据抓取。
- “已确认事实”改为证据 ID 绑定：模型只能引用程序生成的事实目录，未知 ID 直接拒绝，
  页面文字由代码按 ID 还原；模型仍可自由选择重点和组织推断。
- 第二阶段只接收精简确定性事实，但保留全部可得指数与 16 个板块的紧凑多日字段、
  相关性覆盖和重放范围；技术、资金流和财务明细不重复发送。
- DeepSeek 与 GPT 共用两阶段隔离流程；DeepSeek 使用兼容 `/chat/completions` 并关闭
  thinking，GPT 使用 Responses API、低 reasoning、`store:false`，默认模型
  `gpt-5.6-sol`。两次调用在首轮前一次性预留额度，失败后按实际返回调用和 Token 结算。
- 页面顶部新增受控 DeepSeek 全局切换：V4 Flash 默认低成本，V4 Pro 按需选择；对话、
  大盘报告、多股对比、持仓报告、规则草案和事件复核均显式传递实际模型，后端白名单
  拒绝其他值，模型进入各类 7 天缓存键并在结果中显示。
- 已保存持仓列表复用 `/api/watch_quotes` 轻量行情，网页列表精简为标的、最新价/今日
  涨幅、持仓收益率/成本数量三列；只用红绿强调两个百分比，60 秒刷新，也可手动刷新。
- AI 报告页面改为“我的判断、AI 判断、重点问题、一致点、分歧点、可能遗漏”的纵向
  结构，不再重复铺组合金额卡片；最近 7 次成功报告通过单一下拉框复盘。
- AI 请求增加 JSON 输出模式、零随机度、截断检测和单个 JSON 代码块兼容；严格字段
  校验保留。失败响应按可得真实调用/Token 记账，不再统一回滚为 0。
- 结构化持仓报告显式关闭 DeepSeek thinking，避免默认高强度思考占用输出 Token 并
  延长等待；分析边界和严格 JSON 校验保持不变。
- DeepSeek 按需功能默认取消每日调用次数和 Token 上限，只保留本地用量统计；环境
  变量可选重新启用保护，分钟监听仍为 0 Token。
- 报告的免调用缓存仍为 7 天；最近 7 次成功报告独立作为复盘记录保留，直到被新的
  成功报告替换，失败和未完成记录不占复盘名额。
- 多日市场背景增加数据质量门控：缺少完整窗口或近期历史质量不足时显示“数据不足”，
  不参与板块排名；组合集中度重点问题会明确指出实际触发来源，而不是只给笼统等级。

## 测试状态

- 本轮改造后的完整 `unittest discover`：126 项全部通过；其中包含 Flash/Pro 白名单、
  请求透传，以及持仓报告、规则草案和事件复核的跨模型缓存隔离；
- 19 个生产 Python 文件编译通过；当前内嵌 JavaScript Node 语法检查和
  `git diff --check` 通过；
- 自动测试使用 Mock 和临时数据库，没有读取真实凭据或调用真实行情/模型接口；
- 2026-08-10 的 ETF 数据字段使用宽基、行业、主题 ETF 的公开披露人工核对；页面已接入，
  但按用户要求未运行新增自动测试、未启动服务或进行浏览器验收。
- 2026-08-12 分时第一版使用固定分钟数据和假模型完成 37 项定向回归及两个浏览器视口
  检查；没有读取真实 Key、调用真实行情/模型或启动、重启 8688；
- 2026-08-12 按需关键位完成 8 项离线定向测试及一次固定数据宽屏页面交互检查；没有
  调用真实行情/模型、启动或重启 8688，也没有触碰真实盯盘数据库；
- 2026-08-13 双视图完成 49 项定向回归和一次独立端口固定数据宽屏验收；完整套件 152 项
  中 151 项通过，剩余一项为上述固定日期过期的既有持仓报告测试；
- 最新持仓扫描、说明弹窗、相关性覆盖、重放范围和模型切换仍待独立端口的宽屏浏览器
  验收；手机端开发已暂停；

以下为此前版本的历史验收，不代表 2026-08-05 组合分析重构已经完成同等运行时验收：

- 原 `启动.bat`、本地页面、真实大盘和真实 `600519` 查询：通过；
- 宽屏 `1280×720`、窄屏 `390×844` 网页检查：无页面级横向溢出，SVG 非空；
- 浏览器控制台日志采集在清理阶段超时，未据此声明“控制台零错误”。
- 持仓逻辑、事件复核、交易日历和通知重试已通过隔离回归；当时 8688 服务已切换到
  当时代码，并在浏览器 860px 可视宽度下确认盯盘页无横向溢出、空值状态和入口显示正确。
  本轮没有调用真实 AI、行情或微信接口，390px 手机宽度仍需单独补验。
- 自选分组追踪已用固定两轮行情完成 Chromium 端到端验收：宽屏 `1280×720` 与窄屏
  `390×844` 无页面/卡片横向溢出；组均轨迹、同步回暖、改单只分组和旧数据兼容
  均通过，页面脚本与控制台无错误。整组重命名/合并已通过前端契约测试，本轮按
  用户要求优先提交 Git，真实浏览器交互留待后续补验。
- 新版持仓页曾在当时 8688 服务完成宽屏 `1280×720` 和窄屏 `390×844`
  验收：Key/Base URL、微信拖放区、两个操作按钮和空表布局无横向溢出；拖放事件与
  键盘入口已绑定，未选择图片时发送按钮保持禁用，页面控制台无警告或错误。
- AI 持仓报告入口已在同一服务完成宽屏 `1280×720` 和窄屏 `390×844`
  验收：空判断时按钮禁用，填写后启用，输入框与按钮无重叠或横向溢出，控制台无警告
  或错误；测试文字已清除，本轮未点击生成按钮、未读取真实 Key、未调用真实模型。
- 宽屏 `1280×720` 持仓行情列表已用本地 7 只真实持仓验收：`7/7` 行情加载和手动
  刷新成功，行内无溢出，控制台无警告或错误；按用户要求，本轮未调整或验收手机布局。
- 真实库保留 6 条 AI 持仓报告失败记录，最近失败均为模型输出在 JSON 完成前被截断；
  当日已记录 6 次调用、30252 Token，不删除或改写这些复盘证据。当时判断的主要原因是
  `deepseek-v4-pro` 默认高强度 thinking 挤占结构化输出；之后版本已在持仓报告请求中
  显式关闭 thinking，并用请求契约测试锁定。
- 当时 8688 服务已在宽屏 `1000×800` 验收精简持仓列表：本地 7 只持仓行情完整，
  页面和列表无横向溢出，控制台无警告或错误。当前没有成功报告，复盘下拉框为空；
  未伪造报告或再次产生模型费用。浏览器当前显示 DeepSeek Key 未设置，因此关闭
  thinking 后的真实报告仍未复验，不能声明 AI 结论已经生成成功。
- 当时 8688 服务已用真实 `002273` 完成“盘口与概念”验收：接口首次请求约 3.04 秒，
  浏览器缓存查询约 1.3 秒；买一/卖一、量比、换手、振幅、三级行业、主营和 4 个概念
  完整显示，`626×871` 视口无横向溢出，控制台无错误。

## 已知问题与下一步

- ETF 单标的页面已显示跟踪指数、行业配置和前十大持仓；仍待用户在新页面验收宽基、行业、
  主题 ETF 以及空盘口场景。持仓及行业权重需以各自报告期理解，公开端点字段变化时应先以
  固定宽基、行业、主题样例复核。
- ETF 资料读取不再依赖代码识别阶段的名称是否已补全：只要公开资料中有持仓或行业配置，
  就会显示这部分内容；跟踪指数存在时再额外显示指数追踪。
- 分时第一版的固定数据和页面布局已验证；真实盘中数据可达性、免费接口字段稳定性、ETF
  跟踪指数名称匹配及真实 AI 解读质量待用户在原网页验收。分时不可用时主分析应继续显示。
- 双视图固定数据与宽屏交互已验证；真实东方财富日线/换手率接口稳定性、除权场景下的
  价格一致性保护和不同普通股票的实际观感待用户验收。ETF 只显示 K 线结构，不显示可能
  失真的筹码估算；真实 DeepSeek 对新增估算成本密集区事实的解读质量仍待用户主动验收。
1. 先用独立端口和临时数据库完成最新持仓扫描/报告的宽屏网页验收，重点核对
   共同截止日、组合趋势、贡献、集中度、相关性覆盖、强弱分类和历史诊断重放。
2. 用用户真实持仓体验历史诊断重放的可解释性，再决定是否值得升级为需要真实仓位变化、
   交易规则和费用假设的完整策略回测；当前不先做这层复杂化。
3. 仅在用户明确选择并提供额度时，分别做一次 DeepSeek 或 GPT 真实调用，核对页面、
   历史 API、SQLite 状态和实际费用；此前不得声明真实 AI 报告已成功。
4. 优先确认实时价格与收盘分位的展示口径。
5. 将“近 5 年”改为实际起止日/样本数的明确展示。
6. 修复 TLS 校验、动态 HTML 转义和浏览器 Key 风险。
7. 统一 HTTP 错误状态、日志和数据源状态字段。
8. 回归保护扩展后，再按变化频率小步拆分 `app.py`。
9. 用权威交易所日期填充 `data/trading_calendar.json`，确认页面不再显示工作日兜底。
10. 完成真实盘中行情与微信通知验收，服务器部署放在最后。
11. 用实际科技龙头分组观察组均轨迹的可解释性；当前只追踪当天和用户自定义样本，
   不等同于行业指数或跨日趋势。
12. 由用户在本机手工填写千问 Key 和控制台 Base URL，用真实持仓截图验收识别准确率；
    自动测试不读取真实凭据，也不调用百炼。

## 暂时不要做

不要开发手机端，不要更换数据源/框架、把 SQLite 引入 Web 核心、重写前端，或在未确认前修改分位、
负值、前复权、风险评分和接口字段口径。
